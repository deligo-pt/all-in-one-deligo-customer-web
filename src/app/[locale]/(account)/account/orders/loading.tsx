import { AccountSkeleton } from "@/components/shared/skeletons";

/** Orders, inside the account frame the real page renders in. */
export default function Loading() {
  return <AccountSkeleton />;
}
